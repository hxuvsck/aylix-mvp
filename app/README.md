# Aylix App (Expo)

Frontend for Aylix MVP.

Built with Expo (React Native + Web).

---

## Run

```bash
npx expo start

Press:

w → web
scan QR → mobile (Expo Go)
Notes
Uses local API at http://localhost:4000 (web)
Mobile may require local network IP

---

## Option B — delete it (also valid)

If you want ultra-clean:

```bash
rm ~/aylix/aylix-mvp/app/README.md

👉 This is fine because root README already explains everything.

🧠 My recommendation

👉 Keep a short one (Option A)

Because later:

mobile devs understand quickly
repo stays structured
separation of frontend/backend is clear
🚀 Commit
git add app/README.md
git commit -m "docs(app): replace expo default readme"